import 'package:flutter/material.dart';

class MyBrandShowCase extends StatelessWidget {
  const MyBrandShowCase({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(

    );
  }
}
